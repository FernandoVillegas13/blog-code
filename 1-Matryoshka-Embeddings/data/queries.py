# 20 queries de ejemplo sobre tecnologia y programacion
QUERIES = [
    "How do transformers handle long-range dependencies in text?",
    "What is the difference between RAG and fine-tuning an LLM?",
    "Explain how HNSW indexes work for vector search",
    "What are the tradeoffs between cosine similarity and dot product?",
    "How does quantization affect embedding quality?",
    "Best practices for chunking documents in a RAG pipeline",
    "What is the memory wall problem in vector databases?",
    "How do I reduce latency in a semantic search system?",
    "Explain attention mechanisms in simple terms",
    "What is the difference between dense and sparse retrieval?",
    "How does product quantization compress vectors?",
    "When should I use BM25 instead of vector search?",
    "What is mean average precision in information retrieval?",
    "How to evaluate a RAG system in production?",
    "Explain the concept of embedding space alignment",
    "What are matryoshka embeddings and why are they useful?",
    "How does FAISS differ from Qdrant for vector search?",
    "What is a two-stage retrieval pipeline?",
    "How to handle multilingual embeddings in a search system?",
    "What is the role of reranking in modern search systems?",
]

# Documentos del corpus — el sistema buscara dentro de estos
DOCUMENTS = [
    "Transformers use self-attention to relate every token to every other token in a sequence, enabling them to capture long-range dependencies regardless of distance.",
    "RAG retrieves relevant documents at inference time and passes them as context to the LLM, while fine-tuning bakes new knowledge directly into the model weights.",
    "HNSW builds a hierarchical graph where each node connects to its nearest neighbors. Search navigates from coarse top layers down to fine-grained bottom layers, visiting only a fraction of all nodes.",
    "Cosine similarity normalizes by vector magnitude, making it robust to scale differences. Dot product is faster and equivalent when vectors are already normalized to unit length.",
    "Quantization reduces precision (e.g., float32 to int8), shrinking memory footprint and speeding up computation at the cost of some representational fidelity.",
    "Effective chunking strategies include fixed-size with overlap, sentence-based splitting, and semantic chunking. Chunk size should match the granularity of expected queries.",
    "The memory wall occurs when the CPU stalls waiting for data to travel from RAM through the memory bus. In vector search, loading thousands of high-dimensional vectors per query saturates this bandwidth.",
    "To reduce latency: use smaller embeddings for ANN search, apply quantization, reduce the number of candidates passed to reranking, and keep the index in RAM.",
    "Attention computes a weighted sum of values, where weights reflect how relevant each key is to a given query. This allows the model to focus on different parts of the input for each output position.",
    "Dense retrieval uses neural embeddings to capture semantic similarity. Sparse retrieval (BM25) relies on exact term matching and term frequency. Hybrid systems combine both.",
    "Product quantization divides a vector into subvectors and quantizes each independently using a learned codebook, achieving significant compression with controllable quality loss.",
    "Use BM25 when exact keyword matching matters, your corpus is small, or you lack labeled data to train dense embeddings. It is also a strong baseline for hybrid retrieval.",
    "Mean Average Precision (mAP) computes the average precision at each rank position where a relevant result appears, then averages across all queries. It rewards systems that rank relevant results higher.",
    "Evaluate RAG with metrics like faithfulness (answer grounded in context?), answer relevance, and context precision. Tools like RAGAS automate this evaluation.",
    "Embedding space alignment ensures that similar concepts from different modalities or languages map to nearby points. It is achieved through contrastive training on aligned pairs.",
    "Matryoshka embeddings are trained so that the first N dimensions form a valid, high-quality embedding on their own. This allows truncation without retraining, enabling flexible speed-quality tradeoffs.",
    "FAISS is a library optimized for fast CPU/GPU search on flat arrays. Qdrant is a full vector database with filtering, payloads, persistence, and a REST/gRPC API.",
    "A two-stage pipeline first retrieves a large candidate set cheaply (ANN with small vectors), then reranks the top-K with a more expensive but precise scorer (full vectors or a cross-encoder).",
    "For multilingual search, use a model trained on multilingual data such as multilingual-e5 or LaBSE. These map semantically equivalent text in different languages to nearby vectors.",
    "Reranking re-scores a shortlist of candidates using a more powerful model (cross-encoder or full-dimensional dot product). It recovers precision lost during the fast ANN retrieval stage.",
]
